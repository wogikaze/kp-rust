{
  "task": {
    "program": [["main.rs", "{tasklabel}.rs"]],
    "submit": "{tasklabel}.rs",
    "testdir": "tests_{tasklabel}"
  },
  "contest": {
    "static": ["Cargo.toml","Cargo.lock"],
    "cmd": "cargo build --workspace -j 8"
  }
}